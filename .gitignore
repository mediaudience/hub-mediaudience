# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Credenciales de Google Sheets sync
secrets/

# Base de datos y sesiones del backend
server/*.sqlite
server/*.sqlite-*
server/backups/

# Secretos de entorno de producción del backend (SESSION_SECRET, etc.)
# En server/.env, no en la raíz -- ahí Vite lo confundiría con sus propios
# archivos .env por modo (.env.production, etc.) y lo metería en el build.
server/.env
# Logos originales subidos por el usuario (fuente: src/assets/brand/*.png ya extraidos de aca)
Logo Mediaudience/

# Estado local del monitor de salud (no versionar, se regenera solo)
scripts/health-monitor-state.json
